# .NET NativeAOT

Ahead-of-time compilation of C# to native code. No JIT, no runtime dependency. Produces standalone binaries compatible with foreign C ABIs.

## Labs

- [postgres-extension](postgres-extension/) — PostgreSQL C-language extension in pure C#

## Mesh

- Part of [.NET](../README.md)